import 'package:flutter/material.dart';

class inputError extends StatelessWidget {
  const inputError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
